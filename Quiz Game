print("WELCOME TO QUIZ")

play = input("Do you wish to play? Y/N   ")
if play.lower() != "y":
    quit()
print("OK START")
score = 0

ans = input("What is fullform of CPU?  ")
if ans.lower() == "central processing unit":
    print("CORRECT !")
    score += 1
else:
    print("INCORRECT !")

ans = input("What is fullform of GPU?  ")
if ans.lower() == "graphics processing unit":
    print("CORRECT !")
    score = score + 1
else:
    print("INCORRECT !")

ans = input("What is fullform of ALU?  ")
if ans.lower() == "arithmatic logic unit":
    print("CORRECT !")
    score += 1
else:
    print("INCORRECT !")

print ("YOUR SCORE IS : "+ str(score) +".") #try removing the str() around the score. if u dont know why is it there.
